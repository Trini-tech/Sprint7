export interface Starship {
  name: string;
  model: string;
  cost: string;
  speed: string;
  manufacturer: string;
  length: string;
  crew: string;
}

export interface User {
  email: string;
  password: string;
  id: number;
}
